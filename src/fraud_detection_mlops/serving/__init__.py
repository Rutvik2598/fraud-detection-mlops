"""M3 serving: Feast feature store + FastAPI online inference.

The feature layer (``feature_defs``) and the scoring service (``app``) that looks
up online features by card key and scores under the latency budget.
"""
